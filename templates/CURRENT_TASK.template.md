# Current Task

Active task: Sue Agent Lab deployment or operation.

Required routed read:

1. project `BRAIN_HANDOFF.md`
2. project `BRAIN_STATE.json`
3. project records
4. `orchestrator/visible_modes.json`

When the user asks this chat to continue or take over, run the atomic ownership command with this chat's ID and the owner just read. Only the active Main Agent may dispatch, relay, finalize and write authoritative results.

Exact next step: copy from `BRAIN_STATE.json.next_stage` and the handoff. Do not repeat completed stages.
